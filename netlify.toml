[build]
  publish = "."

# Serve static assets directly — never rewrite them
[[redirects]]
  from = "/assets/*"
  to = "/assets/:splat"
  status = 200
  force = true

# SPA fallback for HTML routes only
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
